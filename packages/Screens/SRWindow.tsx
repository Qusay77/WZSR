import PlayerBlock from "packages/UIKit/Player";
import { SRScreen } from "./Blocks";

const SRWindow = () => {
	return (
		<SRScreen>
			<PlayerBlock />
		</SRScreen>
	);
};

export default SRWindow;
